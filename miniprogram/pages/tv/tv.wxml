<view class="container">
  <view class="category">
    <view class="cate-button hot {{selectedHot?'selected':''}}" id="tvHotList" bind:tap="getList">最新热门</view>
    <view class="cate-button zongyi {{selectedZongYi?'selected':''}}" id="tvZongYiList" bind:tap="getList">综艺</view>
    <view class="cate-button jilu {{selectedJiLu?'selected':''}}" id="tvJiLuList" bind:tap="getList">纪录片</view>
  </view>

  <view class="list-conrainer" style="padding-top:100rpx">
    <view class="list-item" wx:for="{{dataList}}" wx:key="id" id="{{item.id}}" bind:tap="getDetail">
      <image class="cover" src="{{item.cover}}" mode="aspectFill" lazy-load="{{true}}"></image>
      <view class="desc">
        <van-tag type="primary" wx:if="{{item.is_new}}">新</van-tag>
        <text class="title">{{item.title}}</text>
        <text class="score">8.8</text>
      </view>
    </view>
  </view>
</view>